# MindBeat audio hooks

Optional short audio files can be placed in this folder with these names:

- `correct.mp3` for a correct answer
- `wrong.mp3` for an incorrect answer
- `streak.mp3` for streak and reward celebrations

The app uses a lightweight Web Audio fallback when a file is not present.